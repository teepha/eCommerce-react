const styles = theme => ({
    buttonProgress: {
        position: 'absolute',
        top: '50%',
        left: '50%',
        marginTop: -12,
        marginLeft: -12,
    },
    submitButtonText: {
        textTransform: "none",
        fontFamily: "Montserrat",
        fontSize: "16px",
        color: "white",
        fontWeight: 700
    }
});

export default styles;
